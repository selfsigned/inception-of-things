apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-3-ingress
spec:
  rules:
  - host: app-3.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: app-3-service
            port:
              number: 80